apiVersion: v1
kind: Service
metadata:
  name: mysqldb
spec:
  type: {{ .Values.mysql.service.type }}
  selector:
    app: mysqldb
  ports:
    - name: mysql
      port: {{ .Values.mysql.service.port }}
      targetPort: 3306
